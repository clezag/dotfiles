# Example shutdown file

# This file is executed as a shell script when labwc is preparing to terminate itself.
# For further details see labwc-config(5).

(source $XDG_RUNTIME_DIR/ssh.env; ssh-agent -k)
